- title    ||= ''
- subtitle ||= ''
- dark ||= false

.title.p-2.text-center.opposite-color
  - if title.present?
    %h1.text-uppercase.mb-0{ class: "#{dark ? "font-color" : "" }" }
      = raw title

  - if subtitle.present?
    %h4.m-0.weight-2{ class: "#{dark ? "font-color" : "" }" }
      = raw subtitle
