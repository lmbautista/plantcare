.navbar.navbar-default{ class: "#{'navmenu-fixed-left' if false}" }
  - if content_for?(:header)
    = yield(:header)
  -else
    .row
      .col-xs-7.col-sm-4.col-md-4.col-md-offset-1.text-center
        %h2.plantcare-icon.mb-2.mt-2
          = link_to I18n.t('title'), root_path

      - if logged?
        = render partial: 'shared/user_profile'
