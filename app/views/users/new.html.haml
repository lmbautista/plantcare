- content_for :header do
  .row
    .col-xs-12.col-md-6
      %h2.plantcare-ico
        = I18n.t('title')
    .col-md-4
      .btn-group.actions
        .signin
          = raw I18n.t('users.new.have_account', signup_link: signin_path)

.signup-title
  .row
    .col-xs-12.col-md-4.col-md-offset-4.text-center
      %h1.uppercase
        = I18n.t('users.new.title')

      %h4
        = I18n.t('users.new.subtitle')        


  .row
    .col-xs-6.col-md-1.col-md-offset-3.text-center
      -# = form_for @user, url: session_path(@user), method: 'post' do |f|
      -#   = f.text_field :username, class: 'form-control'
      -#   = f.password_field :password, class: 'form-control'
      -#   = f.submit, { class: 'btn btn-primary' }
      ASEREJE
