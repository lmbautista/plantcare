%section.intro
  .content
    = render partial: 'shared/title', locals: { title: I18n.t('plantcares.edit.title'), subtitle: @plantcare.name }

    .row
      .col-xs-12.col-md-4.col-md-offset-4.text-center
        = form_for @plantcare, url: plantcare_path(@plantcare), method: put do |f|
          = render partial: 'form', locals: { f: f }
